import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

export const getNegativePricePeriodsService = async () => {
  // Implement the logic to get the longest negative price periods
  return [];
};
